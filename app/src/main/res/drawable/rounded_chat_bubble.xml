<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:rotation="0"
    android:shadowColor="@color/history_list_row_view_heardCount_value"
    android:shadowDx="5"
    android:shadowDy="5"
    android:shadowRadius="5">
    <solid android:color="#FFFFFF" />
    <stroke android:width="5px"
        android:color="@color/ch_counter_border_dark"/><!-- Change color to any background color you like -->
    <corners android:radius="@dimen/_16sdp"/>
    <!-- Round corners -->
    <padding
        android:left="16dp"
        android:top="10dp"
        android:right="16dp"
        android:bottom="10dp" /> <!-- Padding to create space around the text -->
</shape>
